fastapi>=0.109.2
uvicorn>=0.27.1
pandas>=2.2.0
numpy>=1.26.4
boto3>=1.34.40
pydantic>=2.6.1
python-multipart
python-dotenv
requests
# Verifies Supabase access tokens against the project's public JWKS. The [crypto]
# extra is what supplies ES256, which is how new Supabase projects sign.
PyJWT[crypto]>=2.8.0
